#include <vector>
#include <iostream>
#include <iomanip>
#include <ios>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int num_a, num_b, num_t;
    cin >> num_a >> num_b >> num_t;

    vector<int>
}